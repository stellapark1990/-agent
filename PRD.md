# 电商 AI 素材生成 Agent · 产品需求文档

> 版本：v1.1 · 状态：草稿 · 日期：2026-05-31

---

## 目录

1. [背景与目标](#1-背景与目标)
2. [用户分析](#2-用户分析)
3. [竞品分析](#3-竞品分析)
4. [核心结论与产品定位](#4-核心结论与产品定位)
5. [产品功能规格](#5-产品功能规格)
6. [技术架构](#6-技术架构)
7. [成功指标](#7-成功指标)
8. [异常流程设计](#8-异常流程设计)
9. [边界与约束](#9-边界与约束)

---

## 1. 背景与目标

### 1.1 问题陈述

中国电商平台（淘宝/天猫、拼多多、京东）每年新增数以亿计的商品 SKU，中小商家在上架时面临高频、重复且专业门槛极高的素材制作需求。传统路径（外包设计师 / PS 自制 / 模板工具）在成本、效率和合规性上均难以兼顾。

当前电商内容生产存在明显的 **"AI 割裂感"**。对于中小商家（小老板、无专业设计团队的店长）而言，他们需要的不是"生成一张好看的图"，而是"生成一套能直接上架、不会被罚款、不需要打开 PS 修改的完整商品物料"。

核心痛点：

- **出图不可用**：现有 AI 工具生成的图片尺寸不符合平台规范、排版错乱、有压缩画质损耗
- **合规风险高**：电商广告法极其严格（如"最"、"第一"、"绝对"），AI 随意生成的文案极易导致商家被职业打假人盯上或被平台扣分
- **生命周期管理缺失**：商品上架后无更替，遇到换季或节日热点，重新做图成本高，导致转化率随时间下降

### 1.2 产品目标

打造一款面向电商中小商家的 **AI 驱动素材生成 Agent**（产品名：**商图智造**），实现：

- **0 设计基础，1 分钟完成电商主图套图上架**
- 自动完成竞品分析 → 分镜策划 → 图片生成 → 文案撰写 → 合规检测 → 素材迭代全链路
- 以平台审核规范为底线，以营销转化为目标

### 1.3 双核心工作流

```
【工作流一：冷启动极简上架】
商品录入 → 竞品解析 → 全规格生成 → 合规文案 → 一键上架

【工作流二：长线运营智能迭代】
原有素材 → 季节/热点检测 → 自动迭代 → 商家确认更新
```

---

## 2. 用户分析

### 2.1 核心用户

| 用户类型 | 特征描述 | 核心诉求 |
|----------|----------|----------|
| 电商中小商家 | 有货源、有店铺，缺设计能力和时间 | 快速出合格素材，节省外包费用 |
| 个体创业者 | 白手起家，资金有限，一人多职 | 低成本、自助完成上架全套物料 |
| 店铺运营人员 | 有运营经验，不懂 PS / AI 工具 | 快速跟进热点，频繁迭代素材 |

### 2.2 核心痛点

| # | 痛点 | 具体表现 |
|---|------|----------|
| P1 | **素材制作低效** | 一套主图需外包 2-3 天，自制需学 PS，时间成本极高 |
| P2 | **生成素材不可用** | AI 工具生成的图与商品不符、平台审核不通过、图文信息脱节 |
| P3 | **合规风险高** | 广告法违禁词（最、第一、唯一等极限表达）无感知使用，被下架或罚款 |
| P4 | **素材迭代麻烦** | 节假日、大促、热点更新时需重新制作，耗时重复劳动 |
| P5 | **组合图制作空白** | 主图套图（5张成套）逻辑不清，无法自主规划各张分工 |

---

## 3. 竞品分析

### 3.1 主要竞品

| 维度 | 字节跳动·小云雀 | GPT + Midjourney | 本产品（商图智造） |
|------|----------------|-----------------|--------|
| **定位** | 电商营销内容生成 | 通用 AI 创作工具 | 电商素材全链路 Agent |
| **图文一体** | 部分支持 | 需多工具拼接 | 原生支持，一次输入全部输出 |
| **平台规范适配** | 一般 | 无 | 内置淘宝/拼多多/京东规格 |
| **合规检测** | 无 | 无 | 自动违禁词扫描 + LLM 语义级检测 |
| **分镜逻辑** | 无结构化分镜 | 无 | 5 槽位结构化套图模板 |
| **竞品分析输入** | 不支持 | 不支持 | 支持截图/URL 输入竞品分析 |
| **迭代能力** | 手动重新生成 | 手动 | 一键捕捉热点，保留基础素材 |
| **用户门槛** | 中（需理解提示词） | 高（需学习工具链） | 极低（填表单即可） |

### 3.2 竞品深度分析：字节小云雀

- **竞品现状**：底层模型能力强，能生成短视频和图片，在泛营销场景有一定用户基础
- **竞品劣势**：指令遵从性差，缺乏场景化工程打磨。生成的素材往往尺寸不对，文字排版容易出现"鬼画符"，商品形状变形；商家生成后仍需设计师进行二次裁切、修图、改字，并未真正解决问题
- **本产品机会**：专注电商垂直场景的深度工程打磨，确保输出结果"开箱即用"

### 3.3 核心差异化

1. **全链路 vs 单点工具**：覆盖从商品参数录入到合规营销文案的完整闭环
2. **平台规范内化**：图片尺寸、格式、数量等规格以代码形式固化，非人工判断
3. **电商语境**：提示词模板、分镜逻辑、合规检测均为电商场景专属设计

---

## 4. 核心结论与产品定位

### 4.1 关键结论

- 用户最大障碍不是"不会用 AI"，而是"不知道该输入什么"和"生成结果不知道好不好"
- 电商素材的价值链：竞品分析 → 卖点提炼 → 视觉策划 → 图文生产 → 合规把关 → 上架迭代
- 现有工具覆盖链条中间段（图片/文字生成），两端（竞品分析、合规把关、迭代）均为空白

### 4.2 产品定位

> **"0 设计基础、1 分钟完成电商上架"的智能素材 Agent**
>
> 以商品信息为输入，输出可直接用于平台上架的完整主图套图 + 营销文案 + 合规报告。

### 4.3 核心能力

1. 一键生成适配平台的合规营销素材（主图 / 轮播 / 详情图 / 组合图）
2. 自动检测并修改产品卖点违禁信息，确保上架过审
3. 基于季节、热点自动迭代素材，无需人工操作
4. 多 SKU 套餐组合图智能生成，提升套餐视觉转化

### 4.4 预期目标

- **素材制作时间**：从平均 2-3 天压缩至 **5 分钟以内**
- **合规通过率**：AI 审核后的文案广告法违禁词命中率降至 **< 5%**
- **上架可用率**：生成套图中至少 4/5 张符合目标平台主图规格要求
- **用户自助完成率**：首次使用无引导下完成完整流程的比例 **> 70%**
- **零修改直传率**：用户生成素材后未经任何第三方软件（PS/美图等）处理，直接上传至电商平台的比例 **> 80%**

---

## 5. 产品功能规格

### 5.1 整体流程

```
商品录入 (Step 1)
    ↓
特征解析 (Step 2)  ←  AI 自动分析竞品 + 规划分镜
    ↓
效果预览 (Step 3)  ←  图片生成 + 文案 + 合规检测
    ↓
一键捕捉热点 (Step 4)  ←  热点/节日迭代
```

---

### 5.2 Step 1 · 商品录入

**目标**：以最低门槛收集生成所需的结构化输入

#### 输入字段

| 字段 | 必填 | 说明 |
|------|------|------|
| 商品名称 | 是 | 用于 AI 理解商品类目与定位 |
| 分发平台 | 是 | 淘宝/天猫 · 拼多多 · 京东（影响图片规格和审美风格） |
| 商品参数/描述 | 推荐 | 材质、功能、卖点等，支持自由格式文本 |
| 竞品图片 URL | 可选 | 触发 Qwen-VL 多模态竞品视觉分析 |
| 竞品描述 | 可选 | 文字版竞品补充说明 |
| 目标风格 | 可选 | 支持手填或 AI 拓词（调用 `/api/style-suggest`） |
| 推荐卖点 | 可选 | 用于生成营销文案，不填时从商品参数提炼 |

#### AI 风格拓词功能

- 触发方式：点击"目标风格"旁的"AI 拓词"按钮
- 逻辑：基于商品名称 + 平台 + 商品描述，调用 `qwen-turbo` 生成 8-10 个视觉风格词组
- 覆盖维度：氛围感（极简冷淡/温暖治愈）、场景（户外/居家）、色调（莫兰迪/马卡龙）、人群调性（Z世代/精致白领）、风格流派（日系/欧美）
- 交互：词组以 chips 形式展示，点击自动填入"目标风格"字段

#### 平台规格（内化为代码，自动适配）

| 平台 | 主图尺寸 | 主图数量 | 详情图尺寸 | 详情图数量 | 文件大小 |
|------|----------|----------|------------|------------|----------|
| 淘宝/天猫 | 800×800px（推荐≥1000）(1:1) | 5 张 | 宽 750-790 | 8-15 张 | ≤ 3MB |
| 拼多多 | 800×800px (1:1) | 10 张以内 | 宽 750 | 5-20 张 | ≤ 1MB |
| 京东 | 800×800px 起 (1:1) | 5-10 张 | 宽 790 | 10-20 张 | ≤ 3MB |

---

### 5.3 Step 2 · 特征解析

**目标**：AI 自动完成竞品分析、卖点提炼、分镜策划

#### 功能 1：多模态输入与"拆解对标"

- 用户上传商品白底图或输入竞品 URL/截图
- Agent 调用 Vision LLM（Qwen-VL）解析竞品图片的布局、色调、构图逻辑
- 输出样例："竞品采用了 1张白底主图 + 2张场景痛点图（展示便携）+ 2张功效细节图的逻辑，主色调为原木风"
- Agent 将此版式逻辑和分镜脚本套用在用户商品图上进行生成

#### AI 分析输出

| 字段 | 说明 |
|------|------|
| `productTitle` | 综合商品名称与核心卖点的手机端展示标题，≤20字，有冲击力 |
| `seoTitle` | 按淘宝/拼多多/京东搜索习惯排序的关键词优化标题，≤30字，全中文 |
| `competitorInsight` | 竞品分析总结，1-2句话 |
| `productHighlights` | 核心卖点数组，3条 |
| `styleDirection` | 整体风格方向（如户外露营风/极简高端风） |
| `colorScheme` | 主色调建议，含具体十六进制色值 |
| `imageScripts` | 5张图的分镜脚本数组 |

#### 5 槽位套图模板

| Slot | 类型 | 策略目标 | 英文提示词结构 |
|------|------|----------|----------------|
| 1 | 主图（爆款主图） | 展示商品全貌，吸引点击 | Subject + Environment + Lighting & Camera + Style + Negative Prompt |
| 2 | 场景痛点图 | 直击用户痛点，对比强化 | Subject + Visual Contrast + Lighting & Camera + Style |
| 3 | 功能细节图 | 放大核心功能/材质，建立信任 | Subject + Details + Lighting & Camera + Style |
| 4 | 氛围场景图 | 生活方式情绪价值，提升溢价 | Subject + Environment & Props + Lighting & Camera + Style |
| 5 | 白底合规图 | 满足平台搜索审核要求 | Subject + Pure white environment + Flat lighting |

#### AI 模型选择逻辑

```
有竞品图片 URL？
  → 是：先尝试 qwen-vl-plus（多模态）
         ↓ 失败（图片下载失败等）
         → 降级到 qwen-plus（纯文本）
  → 否：直接 qwen-plus（纯文本）
```

---

### 5.4 Step 3 · 效果预览

**目标**：展示完整的可上架素材预览，包含图片、文案、合规状态

#### 功能 2：全套规格自适应生成与裁切

- 根据用户选择的平台，Agent 按照对应规格精准裁切、居中、留白
- 确保商品主体不被文字或边框遮挡，严格输出平台要求的尺寸/格式/大小
- 技术：引入 UI 模板化 + AI 局部重绘，生成大尺寸底图后再精准裁切

#### 布局结构

```
左列：手机端预览              右列：内容详情
  ├─ iPhone 实机框             ├─ 推荐卖点（3条）
  └─ 商品标题（≤20字）         ├─ 图片预览（5张卡片）
                               └─ 营销文案 + 合规徽章
```

#### 图片预览区

- 5张卡片，4:5 比例容器，与手机竖屏比例一致
- 显示 AI 生成图片（调用 `/api/image-process` → wan2.5-i2i 图生图）
- 悬停时显示卡片详情（文案标题/副标题）
- 生成中展示骨架屏动画，缓解等待焦虑

#### 功能 3：合规文案与卖点生成（SSE 流式输出）

1. **`status`**：状态播报（正在生成文案...）
2. **`copies`**：5张图各自的文案（headline / subline / bullets / badgeText）
3. **`status`**：合规检测中...
4. **`compliance`**：
   - `quickHits`：字面违禁词命中（本地词库快速扫描）
   - `semantic`：LLM 语义级合规分析
   - `marketingCopy`：基于核心卖点生成的合规营销正文（60-100字）
5. **`fixedCopies`**（有违禁词时）：自动修复后的文案
6. **`done`**：生成完成

#### 营销文案区

- 展示 LLM 生成的合规营销文案（60-100字，有感染力）
- 附"已通过广告合规检测"绿色徽章（Shield 图标）
- 文案基于 `productHighlights`（推荐卖点）生成，而非违规报告

#### 合规检测双层机制

| 层级 | 方式 | 特点 |
|------|------|------|
| L1 快速扫描 | 本地违禁词词库 (`lib/prohibited-words.ts`) | 毫秒级，字面命中 |
| L2 语义检测 | qwen-plus LLM 审核 | 覆盖隐式违禁表达（如"没人比我便宜"） |
| 自动修复 | 触发条件：L1 或 L2 有命中 | 保持原意和语感，qwen-plus 自动替换 |

> **重要**：违禁词替换必须支持"用户手动撤回或二次修改"，不能完全锁死用户决策权。

---

### 5.5 Step 4 · 一键捕捉热点

**目标**：基于已有素材快速迭代，适应营销热点/季节节点/大促活动

解决商品素材用久了产生"视觉疲劳"和转化下降的问题，采用类似 GitHub 的"分支/版本控制"思路管理商品素材。

#### 触发场景

**季节时令按季巡航（2-3个月周期）**
- Agent 记录商品的原始参数，随着季节交替主动向商家推送迭代建议
- 一键换季：保持商品本体和组合方式不变，仅将素材的背景、色调、文案更新为当前季节元素

**营销热点追击（即时周期）**
- 系统后台接入全网热点库（双11、618、开学季、突发降温等）
- 自动为商品生成带有热点文案的"营销头图/氛围图"，提示商家插入轮播图首张，快速截取热点流量

#### 功能规格

| 功能 | 说明 |
|------|------|
| 热点文案输入 | 用户描述本次迭代方向（如"618大促，主打低价闪购"） |
| 基础素材复用 | 保留原有图片资产，仅更新文案层 |
| 全套重新生成 | 可选择重新走完整流程 |
| 改动日志 | 记录本次迭代变更（文案 diff、风格变化） |
| 对比预览 | 5张图新旧版本并排展示 |

#### 迭代控制区布局（Step 4 右列）

```
迭代控制
  └─ 热点/节日文案输入框
  └─ 重新生成按钮（"更新中..."动态状态）

图片预览（5张）
  └─ 每张卡片：4:5比例 · 悬停显示文案 · 生成中骨架屏

改动日志
  └─ 时间戳 · 变更描述
```

---

## 6. 技术架构

### 6.1 技术栈

| 层级 | 技术选型 |
|------|----------|
| 前端框架 | Next.js App Router（React Server Components + Client Components）|
| 样式系统 | Tailwind CSS v4（`@import "tailwindcss"`） |
| 字体 | Hanken Grotesk（主字体）· Geist Sans · Geist Mono |
| AI 推理 | 阿里云 DashScope（通义千问系列） |
| 图片生成 | wan2.5-i2i-preview（图生图） |
| 流式传输 | SSE（Server-Sent Events）via TransformStream |
| 部署环境 | Node.js Runtime（`export const runtime = "nodejs"`） |

### 6.2 AI 模型矩阵

| 模型 | 用途 | 调用接口 |
|------|------|----------|
| `qwen-plus` | 竞品分析 + 分镜策划 + 文案生成 + 合规检测 | `/api/analyze`, `/api/generate` |
| `qwen-vl-plus` | 多模态竞品图片分析（有竞品图时优先） | `/api/analyze` |
| `qwen-turbo` | 风格词组拓展（轻量任务） | `/api/style-suggest` |
| `wan2.5-i2i-preview` | 商品图生图（场景化处理） | `/api/image-process` |

### 6.3 API 路由

```
POST /api/analyze          → 竞品分析 + 分镜策划（JSON 响应）
POST /api/generate         → 文案生成 + 合规检测（SSE 流式）
POST /api/style-suggest    → AI 风格词拓展（JSON 响应）
POST /api/image-process    → 图片生成（wan2.5 图生图）
```

### 6.4 数据流

```
用户输入
  ↓
/api/analyze
  ├─ qwen-vl-plus（有竞品图）
  └─ qwen-plus（无竞品图/VL降级）
  → AnalysisResult { productTitle, seoTitle, competitorInsight, productHighlights,
                     styleDirection, colorScheme, imageScripts[5] }
  ↓
/api/image-process（并行 × 5）
  → wan2.5-i2i 图生图
  → generatedImages[slot]
  ↓
/api/generate（SSE）
  → copies（5张文案）
  → compliance { marketingCopy, quickHits, semantic }
  → fixedCopies（如有违禁词）
  → done
```

### 6.5 Tailwind v4 注意事项

> **关键限制**：Tailwind v4 会拦截所有 `animate-*` class，导致自定义 CSS `@keyframes` 被覆盖。
>
> 解决方案：所有动画通过 `<style>` JSX 标签内联 `@keyframes`（camelCase 命名），动画效果通过 `style` prop 的 `animation` 属性应用，而非 CSS class。

---

## 7. 成功指标

### 7.1 效率指标

| 指标 | 目标值 | 测量方式 |
|------|--------|----------|
| 首次生成耗时（商品录入→效果预览） | ≤ 3 分钟 | 前端打点 |
| 全套文案生成耗时 | ≤ 30 秒 | SSE 首帧 → done 事件 |
| AI 风格拓词耗时 | ≤ 5 秒 | 按钮点击 → chips 展示 |

### 7.2 质量指标

| 指标 | 目标值 | 测量方式 |
|------|--------|----------|
| 文案违禁词漏检率（经 L1+L2 后） | < 5% | 人工抽查 |
| 生成图片平台规格通过率 | > 90% | 自动校验（尺寸/格式/大小） |
| 商品标题长度合规率（≤20字） | 100% | 代码层截断保障 |

### 7.3 体验指标

| 指标 | 目标值 |
|------|--------|
| 首次使用无引导完成率 | > 70% |
| 零修改直传率（不经 PS 等工具直接上传平台） | > 80% |
| 用户使用 AI 拓词功能比例 | > 40% |
| 迭代功能（Step 4）使用率 | > 30%（首次使用后 2 周内） |

---

## 8. 异常流程设计

### 8.1 输入质量过差

若用户上传的图片光线极暗、模糊不清，引入图像质量检测。若低于阈值，提示："图片过暗/模糊，建议在明亮环境下重新拍摄，否则影响生成质量"。

### 8.2 竞品链接解析失败

若用户输入的竞品链接有防爬虫机制，抓取不到，提供兜底方案：提示"该链接受隐私保护解析失败，可以换个方式或者直接上传竞品截图哦"。自动降级为纯文本模式继续生成。

### 8.3 违禁词语义误判

违禁词替换必须支持"用户手动撤回或二次修改"，不能完全锁死。用户对最终上架内容的合规性有最终决策权。

### 8.4 生成耗时过长

单套素材（5张主图）生成时间可能较长，需加入趣味 Loading 动画缓解焦虑（当前已实现骨架屏动画）。

---

## 9. 边界与约束

### 9.1 当前不支持

- 视频素材生成（仅静态图片）
- 详情页长图生成（仅主图套图 5 张）
- 多 SKU 批量处理
- 直接上传/发布到电商平台（需用户手动下载上传）
- 国际电商平台（Amazon、Shopee 等）

### 9.2 合规边界

- 违禁词检测基于中国广告法，不覆盖平台特定运营规则（如淘宝类目禁用词）
- 图片内容由 AI 生成，不对商品效果与图片的完全一致性做保证
- 用户需对最终上架内容的合规性负责

### 9.3 技术约束

- 图片生成依赖 wan2.5-i2i 模型，需提供有效的基础商品图（base64 输入）
- Qwen-VL 多模态分析需竞品图片 URL 可公开访问（非登录态资源）
- SSE 流式接口需保持 HTTP 长连接，不适合极差网络环境

---

## 附录：产品 Demo 交互剧本

> 以下演示"商图智造"系统的完整用户工作流，以拼多多不锈钢保温杯个体户"西澳"为例。

**Step 1：冷启动上架**

西澳打开软件，点击【新建上架工程】。上传一张手机拍的"不锈钢保温杯"白底照片，输入商品参数："304不锈钢，保温12小时，容量500ml"。

Agent 提问："老板，有没有想对标的同行？"西澳把销量第一的杯子链接粘贴进去。

Agent（思考 loading...）："分析完毕！竞品采用'户外露营风'，主打'便携'卖点。正在为您套用此逻辑生成 5 张 1:1 拼多多规格主图..."

几秒后，界面展示 5 张精美的户外场景图。

**Step 2：违禁词拦截**

西澳检查文案，发现屏幕上跳出一个弹窗。

Agent 提示："西澳，我本来想写'保温效果全网第一'，但触发了《广告法》第 9 条。我已自动帮您修改为'12小时硬核保温'"

西澳很安心，点击【一键打包并发布】。上架完成，且尺寸严丝合缝。

**Step 3：时间快进 3 个月 —— 智能自迭代**

到了 12 月份，大雪。

西澳的微信收到【商图智造】的推送："❄️ 降温预警！您的商品【不锈钢保温杯】当前还是'秋季露营'背景。我已为您生成了【圣诞雪景版】主图，加了'冬日第一杯热水'文案。"

点击链接预览，5 张图全部变成了暖光、松树、雪景背景，产品原样完美融合在里面。

西澳点击【确认更新】，店铺商品图完成换季替换。

---

*本文档基于当前代码库实现状态与产品原始需求文档编写，功能描述与 `/app/page.tsx`、`/app/api/` 路由代码保持一致。*
